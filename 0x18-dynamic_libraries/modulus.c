#include <stdlib.h>
#include <stdio.h>

/**
 * mod - mod of two values
 * @num1: first value
 * @num2: second value
 * Return: modulus of two numbers
 */

int mod(int num1, int num2)
{
	return(num1 % num2);
}
