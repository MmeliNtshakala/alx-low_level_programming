#include <stdlib.h>
#include <stdio.h>

/**
 * subtracts - subtracts two values
 * @num1: first value
 * @num2: second value
 * Return: total of both subtracted values
 */

int subtracts(int num1, int num2)
{
	return(num1 - num2);
}
