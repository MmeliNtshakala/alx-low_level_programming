#include <stdio.h>
/**
 * main - Entry point
 * Discription: 'base in lowercases'
 * Return: always 0
 */
int main(void)
{
	int n;
	int m;

	for (n = 49; n <= 57; n++)
	{
		putchar(n);
	}
	for (m = 97; m <= 102; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
